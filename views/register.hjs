<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/bootstrap.min.css">
    <link rel="stylesheet" href="./styles/style.css">
</head>
<body class="d-flex align-items-center justify-content-center" style="min-height: 100vh; padding: 40px 0;">

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-5">
                <div class="glass-card">
                    <h2 class="text-center mb-4" style="font-weight: 600;">{{title}}</h2>
                    
                    {{#error}}
                    <div class="alert alert-danger" style="border-radius: 10px; font-size: 0.85rem;">{{error}}</div>
                    {{/error}}
                    
                    <form id="registerForm">
                        <div class="form-group mb-3">
                            <label for="name" style="font-weight: 600; font-size: 0.9rem;">Full Name</label>
                            <input type="text" class="form-control" id="name" placeholder="Enter your name" required
                                   style="border-radius: 10px; padding: 10px;">
                        </div>
                        <div class="form-group mb-3">
                            <label for="email" style="font-weight: 600; font-size: 0.9rem;">Email Address</label>
                            <input type="email" class="form-control" id="email" placeholder="Enter email" required
                                   style="border-radius: 10px; padding: 10px;">
                        </div>
                        <div class="form-group mb-3">
                            <label for="password" style="font-weight: 600; font-size: 0.9rem;">Password</label>
                            <input type="password" class="form-control" id="password" placeholder="Min 6 characters" required
                                   style="border-radius: 10px; padding: 10px;">
                        </div>
                        <div class="form-group mb-3">
                            <label for="confirmPassword" style="font-weight: 600; font-size: 0.9rem;">Confirm Password</label>
                            <input type="password" class="form-control" id="confirmPassword" placeholder="Confirm password" required
                                   style="border-radius: 10px; padding: 10px;">
                        </div>
                        <div class="form-group mb-4">
                            <label for="role" style="font-weight: 600; font-size: 0.9rem;">Account Type</label>
                            <select class="form-control" id="role" style="border-radius: 10px; height: 45px;">
                                <option value="customer">Customer</option>
                                <option value="truckOwner">Truck Owner</option>
                            </select>
                        </div>
                        <button type="button" id="submit" class="btn btn-action btn-browse w-100">Register</button>
                    </form>
                    
                    <div class="mt-4 text-center" style="font-size: 0.9rem;">
                        <p class="mb-1">Already have an account? <a href="/login" style="color: var(--accent-orange); font-weight: 600; text-decoration: none;">Login here</a></p>
                        <p class="mb-0"><a href="/" style="color: var(--text-muted); text-decoration: none;">← Back to Home</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/jquery-2.2.0.min.js"></script>
    <script>
        $(document).ready(function(){
            $("#submit").click(function(){
                const name = $("#name").val();
                const email = $("#email").val();
                const password = $("#password").val();
                const confirmPassword = $("#confirmPassword").val();
                const role = $("#role").val();
                
                if(password !== confirmPassword){
                    alert("Passwords do not match!");
                    return;
                }
                
                $.ajax({
                    type: "POST",
                    url: "/api/v1/user",
                    contentType: "application/json",
                    data: JSON.stringify({name: name, email: email, password: password, role: role}),
                    success: function(response){
                        alert("Registration successful! Please login.");
                        window.location.href = "/login";
                    },
                    error: function(xhr){
                        alert("Registration failed: " + (xhr.responseJSON ? xhr.responseJSON.error : "Error creating account"));
                    }
                });
            });
            
            $("#name, #email, #password, #confirmPassword, #role").keypress(function(e){
                if(e.which == 13) $("#submit").click();
            });
        });
    </script>
</body>
</html>